
Exercises FETCH scenario


NamedEntityGraph LIKE JOIN FETCH removes N+1 but introduces duplicates " Cartesian Product"
It does a LEFT OUTER Join so it would pick up parents with NO children

graph is on Member